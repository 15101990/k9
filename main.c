#include <stdio.h>
#include <stdlib.h>
                       //poor readability

int main()
{
    char button,lamp;
    if(button == 0)    //active low pin (the button is pressed)
    lamp=1;

    //if(button==1)
    //lamp=0;
    return 0;
}
